{
  "name": "ja-wp-react-theme",
  "version": "1.0.0",
  "description": "A basic React theme for WordPress.",
  "main": "index.js",
  "scripts": {
    "preview": "npm-run-all --parallel sync start",
    "sync": "browser-sync start -p 'reacttheme.local' --files '**/*.php' 'build/*.js' 'build/*.css'",
    "build": "wp-scripts build",
    "start": "wp-scripts start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Austin Reilly @tinker-dot-bin, Juan Lucero @luceroweb",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/scripts": "^18.0.1",
    "browser-sync": "^2.27.4",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "react-router-dom": "^5.3.0"
  }
}
